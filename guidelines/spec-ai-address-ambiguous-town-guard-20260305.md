# AI住所チェック 町域曖昧入力ガード仕様（2026-03-05）

## 背景
- 町域が1文字など曖昧な入力時に、不自然な修正候補が表示される場合がある。

## スコープ
- 町域入力が曖昧な場合は、補正候補を確定表示しない。
- 郵便番号一致または町域一致がない場合は、補正候補を表示しない。
- 市区町村候補から作る参照候補に、手入力町域を混入しない。

## 受け入れ条件
- 町域が1文字程度の入力で `候補を適用` が出にくくなる。
- 都道府県/市区町村のみ一致のケースで、無関係な町域候補を出さない。
