# KEN_ALL.CSV データ破損修正仕様（2026-03-03）

## 対象
- `KEN_ALL.CSV`
- `backups/KEN_ALL.CSV.*.bak`

## スコープ
- `KEN_ALL.CSV` の都道府県名・市区町村名・町域名で発生している誤変換（例: `１０００葉県`、`３重県`）を全面修正する。
- 破損データは部分置換ではなく、正常な参照ファイル `KEN_ALL - コピー.CSV` を基準に差し替える。

## 制約
- 置換前の `KEN_ALL.CSV` をバックアップ保存する。
- CSVの列構造（郵便番号データ仕様）は保持する。
- アプリ側コードには不要な仕様追加を行わない（データ修復を優先）。

## 受け入れ条件
- 都道府県一覧が正しい47件（`千葉県`、`三重県` を含む）になる。
- 代表的な誤変換語が消える（例: `１０００葉県`、`３重県`、`８戸市`、`３田市`）。
- `npm run build` が成功する。

## 非対象
- 郵便番号データの意味変更（検索アルゴリズムの変更）。
- 住民票モードの未実装補完機能追加。
