# 電話番号 0289 形式対応仕様（2026-03-05）

## 背景
- 固定電話入力時、`0289` で始まる番号を `0289-12-3456` の形式で表示したい。

## スコープ
- 電話番号フォーマット処理に `0289` 専用ルールを追加する。
- 形式: `4桁-2桁-4桁`。

## 制約
- 既存の携帯/固定電話の他フォーマットは維持する。
- 変更は電話番号整形処理のみとする。

## 受け入れ条件
- `0289` で始まる入力が固定電話形式時に `0289-12-3456` で整形される。
- `03/06` やその他既存の整形ルールは変更されない。
