# 電話番号 0289 優先整形修正仕様（2026-03-05）

## 背景
- `0289` 入力途中で `028-9` のように `3-` 区切りが先に適用される。

## スコープ
- 電話番号整形で `0289` 判定を最優先にする。
- 入力モード（携帯/固定）に関係なく `0289` は `4-2-4` 形式を優先する。

## 受け入れ条件
- `0289` で始まる入力は途中入力でも `0289-` 起点で整形される。
- 既存の他番号フォーマットは維持される。
